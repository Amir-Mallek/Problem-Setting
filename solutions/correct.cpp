#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie();
    cout.tie();

    long long a;
    long long b;
    cin >> a >> b;
    cout << a + b << '\n';
    return 0;
}